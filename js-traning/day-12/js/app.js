/**
 * Hands-on bonus — import named PI, add, multiply and default formatPrice from mathUtils.js
 */
import formatPrice, { PI, add, multiply } from "./mathUtils.js";

console.log("Bonus — PI:", PI);
console.log("Bonus — add(2, 3):", add(2, 3));
console.log("Bonus — multiply(4, 5):", multiply(4, 5));
console.log("Bonus — formatPrice(99):", formatPrice(99));
